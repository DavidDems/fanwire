# Category 3 (scripted execution) per wiki/GeneralContext/UsageRules/AgentType/3-scripted-execution.md.
# Trigger: push to /tests. Model: cheapest tier that passes. Context: task-scoped
# (branch/PR diff) only. Output: distilled result to wiki/GeneralContext/Reports/test-runs/, fixed
# low-token format — never feed raw logs back into an interactive agent's context.
#
# TODO: not yet implemented. No test runner exists until the backend/frontend
# codebase is scaffolded (see AGENTS.md "Build / test / run").
name: test-agent

on:
  push:
    paths:
      - "tests/**"

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODO - no test runner defined yet"
