; Agent-infrastructure test suite. Separate from backend/frontend/infra suites
; on purpose: `.ai/` is the machinery that runs the application's suites, so it
; must be testable without any application dependency installed.
[pytest]
testpaths = tests
