Agent tooling scaffold per `wiki/GeneralContext/UsageRules/index.md`'s repo layout diagram. Empty until the actual agent/tooling stack for this repo is chosen — `settings.json`, `skills/`, and `hooks/` are placeholders, not populated yet, to avoid speculative config that may not match whatever tool ends up running here (YAGNI, `wiki/CodeContext/Standards/design-principles.md`).
